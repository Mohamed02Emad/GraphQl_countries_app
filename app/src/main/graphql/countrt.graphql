query Country($code : ID!){
    country(code : $code) {
        name
        emoji
        code
        capital
        currency
        languages {
            name
        }
        continent {
            name
        }
    }
}